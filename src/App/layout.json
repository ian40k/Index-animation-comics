export const metadata = {
  title: 'Animation & Comics Portfolio',
  description: 'A portfolio website for animations and comics',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  )
}
